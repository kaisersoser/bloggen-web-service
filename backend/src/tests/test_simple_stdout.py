"""Legacy stdout capture tests retired in favor of native CrewAI callbacks."""

import pytest

pytest.skip(
    "Legacy stdout capture path removed; coverage lives in test_blog_event_listener.py.",
    allow_module_level=True,
)