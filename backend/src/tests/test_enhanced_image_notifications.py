"""Legacy enhanced image notification test retired with stdout capture removal."""

import pytest

pytest.skip(
    "Legacy stdout-capture based enhanced image notification test retired; callback instrumentation supersedes it.",
    allow_module_level=True,
)